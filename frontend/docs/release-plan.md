# Release Plan
1. Enable `shellV2` for internal canary users.
2. Enable `executionV2` and monitor SSE reconnect warnings.
3. Enable `researchV2` and verify compare funnel usage.
4. Enable `runDetailV2` and `compareV2` for analysts.
5. Remove legacy UI toggles after two release cycles.
